/**
 * @todo Point d'entrée pour faire vos exercices...
 */
window.onload = init

function init() {
    console.log("Page initialisée, on va maintenant rendre les éléments dynamiques...")

    let API = new AnimeEstiamApi("test")
}